# Production audio ML pipeline — Python 3.10
# Pin exact versions for reproducible builds. Re-verify compatibility before bumping torch/torchaudio together.

# --- Web / API layer ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1
pydantic==2.10.4

# --- ML / audio processing ---
# torch and torchaudio versions must stay in lockstep (torchaudio 2.5.x pairs with torch 2.5.x)
torch>=2.0.0
torchaudio==2.5.1
librosa==0.10.2.post1
scipy==1.14.1
numpy==1.26.4
silero-vad==5.1.2
